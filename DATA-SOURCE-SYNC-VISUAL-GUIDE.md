# 🎨 Data Source Sync - Visual Guide

A visual walkthrough of the Data Source Sync system.

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         Frontend (React/Next.js)                │
│                                                                 │
│  ┌────────────────────────────────────────────────────────┐   │
│  │           DataSourceManager Component                   │   │
│  │  • View all data sources                                │   │
│  │  • Trigger manual syncs                                 │   │
│  │  • Monitor sync status                                  │   │
│  │  • Create/Edit/Delete sources                          │   │
│  └────────────────────────────────────────────────────────┘   │
│                            ↕ HTTP/REST API                     │
└─────────────────────────────────────────────────────────────────┘
                                 ↓
┌─────────────────────────────────────────────────────────────────┐
│                      Backend (NestJS)                           │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │              Sync Controller                             │  │
│  │  POST /sync/test-connection                              │  │
│  │  POST /sync/data-sources/:id                             │  │
│  │  GET  /sync/supported-sources                            │  │
│  └──────────────────┬──────────────────────────────────────┘  │
│                     ↓                                           │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │         DataSourceSyncService                            │  │
│  │  • Orchestrates all sync operations                      │  │
│  │  • Manages adapter registry                              │  │
│  │  • Handles document matching                             │  │
│  │  • Tracks statistics                                     │  │
│  └──────────────────┬──────────────────────────────────────┘  │
│                     ↓                                           │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │         SyncSchedulerService                             │  │
│  │  • Cron-based scheduling                                 │  │
│  │  • Hourly/Daily/Weekly frequencies                       │  │
│  │  • Automated sync triggers                               │  │
│  └─────────────────────────────────────────────────────────┘  │
│                     ↓                                           │
│  ┌────────┬──────────┬──────────┬──────────────────┐         │
│  │ Google │Confluence│  GitHub  │     Notion       │         │
│  │ Drive  │ Adapter  │ Adapter  │     Adapter      │         │
│  │ Adapter│          │          │                  │         │
│  └────────┴──────────┴──────────┴──────────────────┘         │
│       ↓         ↓          ↓              ↓                    │
└───────────────────────────────────────────────────────────────┘
        ↓         ↓          ↓              ↓
┌─────────────────────────────────────────────────────────────────┐
│                    External Platforms                           │
│  • Google Drive    • Confluence Cloud                           │
│  • GitHub          • Notion                                     │
└─────────────────────────────────────────────────────────────────┘
```

## 🔄 Sync Flow Diagram

```
┌──────────────┐
│   Trigger    │  ← Manual or Scheduled
└──────┬───────┘
       ↓
┌──────────────────────────────────────────┐
│  Get Data Source Configuration           │
│  • Source type (github, confluence, etc) │
│  • Credentials (tokens, API keys)        │
│  • Config (repos, spaces, filters)       │
└──────┬───────────────────────────────────┘
       ↓
┌──────────────────────────────────────────┐
│  Select Appropriate Adapter              │
│  Based on source type                    │
└──────┬───────────────────────────────────┘
       ↓
┌──────────────────────────────────────────┐
│  Adapter Fetches Documents               │
│  • Connect to external platform          │
│  • Apply filters (path, type, etc)       │
│  • Extract content and metadata          │
│  • Convert to standard format            │
└──────┬───────────────────────────────────┘
       ↓
┌──────────────────────────────────────────┐
│  Match with Existing Documents           │
│  • Compare by external ID                │
│  • Check modification dates              │
│  • Determine action needed               │
└──────┬───────────────────────────────────┘
       ↓
       ├────────────────┬────────────────┬────────────────┐
       ↓                ↓                ↓                ↓
┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐
│  Create  │    │  Update  │    │  Delete  │    │   Skip   │
│   New    │    │ Modified │    │ Removed  │    │Unchanged │
└────┬─────┘    └────┬─────┘    └────┬─────┘    └────┬─────┘
     ↓               ↓               ↓               ↓
┌────────────────────────────────────────────────────────────┐
│           Process Each Document                            │
│  1. Save to database                                       │
│  2. Chunk text (LangChain)                                 │
│  3. Generate embeddings (Ollama)                           │
│  4. Index in vector store (Qdrant)                         │
└────┬───────────────────────────────────────────────────────┘
     ↓
┌────────────────────────────────────────────────────────────┐
│           Update Sync Statistics                           │
│  • Documents processed                                     │
│  • Documents added/updated/deleted                         │
│  • Errors encountered                                      │
│  • Timestamp                                               │
└────┬───────────────────────────────────────────────────────┘
     ↓
┌────────────────────────────────────────────────────────────┐
│           Update Data Source Status                        │
│  • Status: active, error, syncing                          │
│  • lastSyncedAt timestamp                                  │
│  • Error message (if any)                                  │
└────────────────────────────────────────────────────────────┘
     ↓
   Done! ✅
```

## 📊 Data Model

```
┌─────────────────────────────────────────┐
│         DataSource Entity               │
├─────────────────────────────────────────┤
│ id: uuid                                │
│ organizationId: string                  │
│ sourceType: string                      │  ← github, confluence, etc
│ name: string                            │
│ credentials: jsonb (encrypted)          │
│ config: jsonb                           │
│ syncFrequency: string                   │  ← hourly, daily, manual
│ lastSyncedAt: timestamp                 │
│ status: string                          │  ← active, error, syncing
│ errorMessage: string?                   │
│ isEnabled: boolean                      │
└─────────────────┬───────────────────────┘
                  │ 1:N
                  ↓
┌─────────────────────────────────────────┐
│         Document Entity                 │
├─────────────────────────────────────────┤
│ id: uuid                                │
│ organizationId: string                  │
│ dataSourceId: uuid?                     │  ← Links to data source
│ title: string                           │
│ content: text                           │
│ contentType: string                     │
│ url: string                             │
│ metadata: jsonb                         │  ← Includes externalId
│   └─ externalId: string                 │  ← Platform's ID
│ chunkCount: int                         │
│ processingStatus: string                │
│ tags: array                             │
│ version: int                            │  ← Incremented on update
└─────────────────┬───────────────────────┘
                  │ 1:N
                  ↓
┌─────────────────────────────────────────┐
│      DocumentChunk Entity               │
├─────────────────────────────────────────┤
│ id: uuid                                │
│ documentId: uuid                        │
│ content: text                           │
│ chunkIndex: int                         │
│ metadata: jsonb                         │
│ embedding: vector (in Qdrant)          │
└─────────────────────────────────────────┘
```

## 🎯 Platform Connection Flow

### GitHub Example

```
1. User Input                    2. Test Connection
┌────────────────┐              ┌────────────────┐
│ Source Type:   │              │ Verify Token   │
│   GitHub       │              │   ↓            │
│ Access Token:  │   →  Test  → │ Try API Call   │
│   ghp_xxx      │              │   ↓            │
│ Config:        │              │ Return Success │
│   owner: user  │              └────────────────┘
│   repo: docs   │                      ↓
└────────────────┘              3. Create Source
                                ┌────────────────┐
                                │ Save to DB     │
                                │ Status: active │
                                └────────────────┘
                                        ↓
                                4. First Sync
                                ┌────────────────┐
                                │ Fetch Files    │
                                │   ↓            │
                                │ Process Each   │
                                │   ↓            │
                                │ Index Vectors  │
                                └────────────────┘
```

## 📅 Scheduling System

```
Cron Scheduler (NestJS Schedule)
        ↓
┌───────────────────────────────────────────┐
│         Every Hour                        │
│  Check for sources with:                  │
│  • syncFrequency = 'hourly'               │
│  • lastSyncedAt > 1 hour ago              │
│  • isEnabled = true                       │
└───────────────┬───────────────────────────┘
                ↓
        Trigger Sync
                ↓
┌───────────────────────────────────────────┐
│         Every Day (Midnight)              │
│  Check for sources with:                  │
│  • syncFrequency = 'daily'                │
│  • lastSyncedAt > 24 hours ago            │
│  • isEnabled = true                       │
└───────────────┬───────────────────────────┘
                ↓
        Trigger Sync
                ↓
┌───────────────────────────────────────────┐
│         Manual Trigger                    │
│  User clicks "Sync Now"                   │
│  API: POST /sync/data-sources/:id         │
└───────────────┬───────────────────────────┘
                ↓
        Trigger Sync Immediately
```

## 🎨 UI Components

### DataSourceManager Component

```
┌──────────────────────────────────────────────────────────┐
│  📁 Data Sources                           [+ Add Source] │
├──────────────────────────────────────────────────────────┤
│                                                           │
│  ┌─────────────────┐  ┌─────────────────┐  ┌──────────┐ │
│  │ 🐙 GitHub Docs  │  │ 🌐 Confluence   │  │ 📝 Notion│ │
│  │ ───────────────│  │ ───────────────│  │ ─────────│ │
│  │ Status: ✅      │  │ Status: 🔄      │  │ Status: ❌│ │
│  │ Active          │  │ Syncing         │  │ Error    │ │
│  │                 │  │                 │  │          │ │
│  │ Frequency:      │  │ Frequency:      │  │ Frequency│ │
│  │   Daily         │  │   Hourly        │  │   Daily  │ │
│  │                 │  │                 │  │          │ │
│  │ Last Synced:    │  │ Last Synced:    │  │ Error:   │ │
│  │   2 hours ago   │  │   5 mins ago    │  │   Auth   │ │
│  │                 │  │                 │  │   failed │ │
│  │ [🔄 Sync]       │  │ [🔄 Sync]       │  │ [🔄 Sync]│ │
│  │ [⚙️ ] [🗑️ ]     │  │ [⚙️ ] [🗑️ ]     │  │ [⚙️][🗑️]│ │
│  └─────────────────┘  └─────────────────┘  └──────────┘ │
│                                                           │
└──────────────────────────────────────────────────────────┘
```

### Create Data Source Modal

```
┌──────────────────────────────────────────────────────┐
│  Add Data Source                          [✕]        │
├──────────────────────────────────────────────────────┤
│                                                       │
│  Select Platform:                                    │
│                                                       │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐          │
│  │ 🐙 GitHub│  │🌐Conflue │  │📝 Notion │          │
│  │          │  │  nce     │  │          │          │
│  └──────────┘  └──────────┘  └──────────┘          │
│                                                       │
│  ┌──────────┐  ┌──────────┐                         │
│  │☁️ Google │  │ + Custom │                         │
│  │  Drive   │  │          │                         │
│  └──────────┘  └──────────┘                         │
│                                                       │
│                               [Cancel] [Continue →]  │
└──────────────────────────────────────────────────────┘
```

## 🔐 Security Flow

```
┌──────────────────────────────────────────┐
│     User Provides Credentials            │
│  • API Token                             │
│  • OAuth Access/Refresh Token            │
│  • API Key                               │
└──────┬───────────────────────────────────┘
       ↓
┌──────────────────────────────────────────┐
│     Backend Validates Format             │
│  • Check required fields                 │
│  • Validate structure                    │
└──────┬───────────────────────────────────┘
       ↓
┌──────────────────────────────────────────┐
│     Test Connection                      │
│  • Make test API call                    │
│  • Verify permissions                    │
└──────┬───────────────────────────────────┘
       ↓ Success
┌──────────────────────────────────────────┐
│     Encrypt Credentials                  │
│  • Use encryption key from env           │
│  • Store in database as JSONB            │
└──────┬───────────────────────────────────┘
       ↓
┌──────────────────────────────────────────┐
│     Association with Organization        │
│  • Link to organization ID               │
│  • Never expose in API responses         │
└──────────────────────────────────────────┘
       ↓
┌──────────────────────────────────────────┐
│     Use for Syncing                      │
│  • Decrypt when needed                   │
│  • Use only in backend                   │
│  • Log access for audit                  │
└──────────────────────────────────────────┘
```

## 📈 Status States

```
Data Source Lifecycle:

  [Created] ──────→ [Active]
      ↓                ↓
      │                ├──→ [Syncing] ──→ [Active]
      │                │         ↓
      │                │    [Error] ←──────┐
      │                │         ↓         │
      │                └──→ [Paused]       │
      │                      ↓             │
      └──────────→ [Deleted]              │
                                           │
  Retry on next scheduled sync ───────────┘

Status Indicators:
  ✅ Active   - Ready and working
  🔄 Syncing  - Currently syncing
  ❌ Error    - Last sync failed
  ⏸️  Paused   - Temporarily disabled
  🗑️  Deleted  - Removed (with documents)
```

## 🎯 Document Matching Logic

```
For each external document:

┌────────────────────────────────────┐
│  Get External ID from platform     │
└────────┬───────────────────────────┘
         ↓
┌────────────────────────────────────┐
│  Search DB for matching externalId │
│  in metadata                       │
└────────┬───────────────────────────┘
         ↓
    ┌────┴─────┐
    │  Found?  │
    └────┬─────┘
         │
    No ←─┴─→ Yes
    ↓         ↓
┌──────┐  ┌──────────────────┐
│CREATE│  │ Compare Dates    │
└──────┘  └────┬─────────────┘
             ↓
        ┌────┴─────┐
        │Modified? │
        └────┬─────┘
             │
        No ←─┴─→ Yes
        ↓         ↓
     ┌──────┐  ┌──────┐
     │ SKIP │  │UPDATE│
     └──────┘  └──────┘

After all documents processed:

┌────────────────────────────────────┐
│  Check for documents in DB not     │
│  found in external source          │
└────────┬───────────────────────────┘
         ↓
    ┌────┴─────────┐
    │ syncDeletes  │
    │   enabled?   │
    └────┬─────────┘
         │
    No ←─┴─→ Yes
    ↓         ↓
 ┌──────┐  ┌──────┐
 │ KEEP │  │DELETE│
 └──────┘  └──────┘
```

## 🚀 Performance Optimization

```
Sync Strategies:

1. Full Sync (First time)
   ┌─────────────────────────────┐
   │ Fetch ALL documents         │
   │ Process each one            │
   │ Index in vector store       │
   └─────────────────────────────┘
   ⏱️ Slow but thorough

2. Incremental Sync (Subsequent)
   ┌─────────────────────────────┐
   │ Use lastSyncedAt timestamp  │
   │ Fetch only modified docs    │
   │ Process changes only        │
   └─────────────────────────────┘
   ⏱️ Fast and efficient

3. Batch Processing
   ┌─────────────────────────────┐
   │ Group documents             │
   │ Process in parallel         │
   │ Limit concurrent operations │
   └─────────────────────────────┘
   ⏱️ Balanced performance

Rate Limiting:
   Platform API → [Queue] → Process
                     ↓
              Respect limits
              Exponential backoff
```

## 📊 Monitoring Dashboard (Future)

```
┌──────────────────────────────────────────────────────┐
│  Sync Activity Dashboard                             │
├──────────────────────────────────────────────────────┤
│                                                       │
│  Last 24 Hours:                                      │
│  ████████████████░░░░░░ 420 documents synced        │
│                                                       │
│  ┌─────────────────────┐  ┌─────────────────────┐  │
│  │ Total Sources: 8    │  │ Active Syncs: 2     │  │
│  └─────────────────────┘  └─────────────────────┘  │
│                                                       │
│  ┌─────────────────────┐  ┌─────────────────────┐  │
│  │ Success Rate: 98%   │  │ Errors Today: 3     │  │
│  └─────────────────────┘  └─────────────────────┘  │
│                                                       │
│  Recent Syncs:                                       │
│  ✅ GitHub Docs      - 5 mins ago - +3 docs         │
│  ✅ Confluence Wiki  - 1 hour ago - +12 docs        │
│  ❌ Notion Pages     - 2 hours ago - Auth error     │
│  ✅ Google Drive     - 3 hours ago - +1 doc         │
│                                                       │
└──────────────────────────────────────────────────────┘
```

---

## 🎓 Learning Path

```
1. Start Here
   └─→ Quick Start Guide

2. Understand Concepts
   └─→ This Visual Guide

3. Implementation Details
   └─→ Full Documentation

4. Hands-On Practice
   └─→ Run Test Script

5. Customize
   └─→ Create Custom Adapter
```

---

**Ready to get started?** → [Quick Start Guide](./DATA-SOURCE-SYNC-QUICK-START.md)
