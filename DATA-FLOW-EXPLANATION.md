# 📊 Complete Data Flow Explanation

## 🎯 How Data Flows from Node to Node

### Step-by-Step Visual Flow

```
┌─────────────────────────────────────────────────────────────┐
│ 1. USER ENTERS TEST DATA                                    │
├─────────────────────────────────────────────────────────────┤
│ Test Modal:                                                  │
│ {                                                            │
│   "message": "Qui est Walid HASSANI?",                      │
│   "userId": "test-123"                                       │
│ }                                                            │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│ 2. FRONTEND SENDS TO BACKEND                                │
├─────────────────────────────────────────────────────────────┤
│ POST /api/workflows/:id/execute                             │
│ Body: {                                                      │
│   triggerData: {                                            │
│     message: "Qui est Walid HASSANI?",                      │
│     userId: "test-123"                                       │
│   },                                                         │
│   context: {}                                               │
│ }                                                            │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│ 3. BACKEND CREATES EXECUTION CONTEXT                        │
├─────────────────────────────────────────────────────────────┤
│ const context = {                                           │
│   triggerData: {                                            │
│     message: "Qui est Walid HASSANI?",                      │
│     userId: "test-123",                                      │
│     organizationId: "..."                                    │
│   },                                                         │
│   variables: {                                              │
│     organizationId: "..."                                    │
│   },                                                         │
│   stepOutputs: {}  ← EMPTY initially                        │
│ }                                                            │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│ 4. TRIGGER NODE EXECUTES                                    │
├─────────────────────────────────────────────────────────────┤
│ Input: context (has triggerData)                            │
│                                                              │
│ TriggerNodeExecutor.execute():                              │
│   return {                                                   │
│     success: true,                                          │
│     data: {                                                 │
│       triggered: true,                                      │
│       triggerData: context.triggerData  ← Passes it along  │
│     }                                                        │
│   }                                                          │
│                                                              │
│ Output stored in context:                                   │
│ context.stepOutputs['node_0'] = {                           │
│   success: true,                                            │
│   data: {                                                   │
│     triggered: true,                                        │
│     triggerData: {                                          │
│       message: "Qui est...",                                │
│       userId: "..."                                          │
│     }                                                        │
│   }                                                          │
│ }                                                            │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│ 5. AGENT NODE EXECUTES                                      │
├─────────────────────────────────────────────────────────────┤
│ Input: SAME context object                                  │
│                                                              │
│ AgentNodeExecutor.execute(node, context):                   │
│                                                              │
│ Available data:                                             │
│ ┌─────────────────────────────────────────────────────┐   │
│ │ A. context.triggerData                               │   │
│ │    {                                                 │   │
│ │      message: "Qui est...",                          │   │
│ │      userId: "...",                                  │   │
│ │      organizationId: "..."                           │   │
│ │    }                                                 │   │
│ └─────────────────────────────────────────────────────┘   │
│                                                              │
│ ┌─────────────────────────────────────────────────────┐   │
│ │ B. context.stepOutputs                               │   │
│ │    {                                                 │   │
│ │      node_0: {                                       │   │
│ │        success: true,                                │   │
│ │        data: {                                       │   │
│ │          triggered: true,                            │   │
│ │          triggerData: {...}  ← Also here!            │   │
│ │        }                                             │   │
│ │      }                                               │   │
│ │    }                                                 │   │
│ └─────────────────────────────────────────────────────┘   │
│                                                              │
│ ┌─────────────────────────────────────────────────────┐   │
│ │ C. context.variables                                 │   │
│ │    {                                                 │   │
│ │      organizationId: "..."                           │   │
│ │    }                                                 │   │
│ └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│ 6. AGENT TRIES TO GET PROMPT                                │
├─────────────────────────────────────────────────────────────┤
│ Code: getInputValue('message', context)                     │
│                                                              │
│ Checks in order:                                            │
│ 1. context.stepOutputs['message']                           │
│    ❌ Not found (stepOutputs has 'node_0', not 'message')  │
│                                                              │
│ 2. context.variables['message']                             │
│    ❌ Not found (variables has 'organizationId', not 'message')│
│                                                              │
│ 3. context.triggerData['message']                           │
│    ✅ SHOULD FIND IT HERE!                                  │
│    Value: "Qui est Walid HASSANI?"                         │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│ 7. IF WORKING CORRECTLY                                     │
├─────────────────────────────────────────────────────────────┤
│ triggerPrompt = "Qui est Walid HASSANI?"                   │
│ finalPrompt = triggerPrompt                                 │
│                                                              │
│ Agent sends to LLM:                                         │
│ {                                                            │
│   model: "mistral",                                         │
│   messages: [                                               │
│     {role: "system", content: "generaliste"},              │
│     {role: "user", content: "Qui est Walid HASSANI?"}  ←  │
│   ]                                                          │
│ }                                                            │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔍 The Problem

The code SHOULD work because:

1. ✅ Test data goes into `triggerData`
2. ✅ `getInputValue` checks `context.triggerData`
3. ✅ Should find your message

But it's NOT working, which means either:
- A) `triggerData` is not being set correctly
- B) `getInputValue` is not checking `triggerData` properly
- C) The data structure is different than expected

---

## 🧪 What I Need to See

Please run the test and share the console output showing:

```javascript
Agent Executor Context:
  triggerData: {...}  ← What's here?
  variables: {...}
  stepOutputs: [...]
  triggerPrompt found: ???  ← Is it null or your message?
  FINAL PROMPT: ???  ← What does it actually use?
```

This will tell me EXACTLY where the problem is!

---

## 💡 Quick Fix Option

While we debug, you can modify the trigger node to output your data differently:

```typescript
// In trigger-node.executor.ts, change to:
return {
  success: true,
  data: {
    ...context.triggerData,  // ← Spread trigger data into output
    triggered: true,
    timestamp: new Date().toISOString(),
  },
};
```

This way your `message` would be directly in `stepOutputs['node_0'].data.message` and accessible!

---

**Please run the test one more time and share the "Agent Executor Context" logs so I can see exactly what's happening!**
